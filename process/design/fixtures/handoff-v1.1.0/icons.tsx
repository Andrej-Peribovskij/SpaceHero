/**
 * Glyphs this version needs.
 *
 * `wave` heads the greeting. It is not in the app's current icon set — the design tool pulled
 * it straight from the icon library. `user` is there already.
 */
import { Hand, User } from "lucide-react";

export const icons = { wave: Hand, user: User };
