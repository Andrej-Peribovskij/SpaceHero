# Greeting — handoff export

Exported from the design tool for engineering handoff.

**Version intent:** adds a greeting surface to the starter app. One new screen, no change to
any existing screen. The design language is unchanged from v1.0.0.

## Screens

| Screen | Purpose |
|---|---|
| `screens/GreetingScreen.tsx` | the landing surface: who is here, and a way in |

## Notes for engineering

- The greeting card is new. Nothing like it exists in the current app.
- The chip primitive is included so the export renders standalone.
- The header wants a waving-hand glyph.
- Data is placeholder. Shape is indicative, values are not.
