---
name: "Design: Preview & Export"
description: Preview a -tech revision live and export it — or the production version that ships today — back to the design tool as a readable source tree (designer)
category: Workflow
tags: [design-process]
---

<!--
  A thin shim so /design:design-04-preview-export works in Claude Code. It carries no
  bindings: the wrapper it includes is the single source for them, and the wrapper names the
  prompt under process/design/prompts/. Other clients include design-commands/ directly and
  never read this file. Hand-written — nothing regenerates it.
-->

@design-commands/design-04-preview-export.md
